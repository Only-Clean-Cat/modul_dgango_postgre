# 1 Скачиваем и устанавливаем на ПК программу PostgreSQL c https://www.postgresql.org/

# 2 В терминале проекта устанавливаем дрова через команду
pip install psycopg2

# 3 В папке проекта в файле settings.py меняем DATABASES на
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': "[YOUR_DATABASE_NAME]",
        'USER': "[YOUR_USER_NAME]",
        'PASSWORD': "",
        'HOST': "localhost",
        'PORT': "",
    }
}
# и вносим свои параметры
